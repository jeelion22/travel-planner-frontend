// import { instance, protectedInstance } from "./instance";

// const userServices = {
//   register: async (userRegisterData) =>
//     await instance.post("/users/register", userRegisterData),
//   verify: async (userId, otp) => instance.post(`/users/verify/${userId}`, otp),
// };

// export default userServices;
